name: TallyUp
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  shared_preferences: ^2.2.2
  google_mobile_ads: ^5.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.13.1  # 👈 BU SATIRI EKLE

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
    - assets/icons/kurdistan_flag.png


flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.png"  # 👈 PNG ikon dosyanın yolu